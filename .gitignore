# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# npm install (the remote-session hook) writes this; bun.lock is the
# lockfile of record — see CLAUDE.md.
package-lock.json
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# The logo contact sheet is a thing you look at once, regenerated by
# `npm run logo-sheet` whenever the logos change.
logo-sheet.png
